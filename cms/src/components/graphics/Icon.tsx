import React from 'react'

// Small mark shown in the admin nav header (replaces the Payload icon).
export default function Icon() {
  return (
    <img
      src="/srkr-logo.png"
      alt="SRKR"
      style={{ height: 28, width: 'auto' }}
    />
  )
}
